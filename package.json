{
  "name": "otonom-fabrika",
  "version": "1.0.0",
  "private": true,
  "description": "AI agent development framework — copy this repo to start a new project",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.x"
  },
  "packageManager": "npm@10.8.1"
}
